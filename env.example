# HRMS Lite – production env (MongoDB Atlas). Copy to .env and set values.
# cp env.example .env

# MongoDB Atlas connection string (required)
MONGODB_URL=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=hrms_lite

# Backend security (required in production)
SECRET_KEY=your-production-secret-key-at-least-32-characters-long

# CORS: comma-separated origins that will call the API (frontend URL, VM IP, hostname)
# For VM access use both IP and hostname with your FRONTEND_PORT (e.g. 3002)
ALLOWED_ORIGINS=https://your-domain.com,http://16.16.203.101:3002,http://ec2-16-16-203-101.eu-north-1.compute.amazonaws.com:3002

# Optional (default 3002; use 80 if no reverse proxy)
FRONTEND_PORT=3002
